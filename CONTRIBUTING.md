# Contributing

## 변경 흐름

1. 실제 제품에서 반복되는 UI 문제인지 확인합니다.
2. 색상과 크기를 컴포넌트에 직접 넣기 전에 기존 `--co-*` 토큰으로 표현 가능한지 확인합니다.
3. 컴포넌트 API는 제품 용어가 아니라 UI 역할로 이름 짓습니다. 예: `CallCard` 대신 `Card`.
4. 키보드 포커스, 모바일 폭, 모션 감소 환경을 함께 구현합니다.
5. 패키지를 빌드하고 최소 한 개의 소비 앱에서 타입 검사와 렌더링 테스트를 통과시킵니다.

## 변경 기준

- 패치: 시각적 오류, 접근성 개선, 타입 보강
- 마이너: 호환되는 토큰·컴포넌트·prop 추가
- 메이저: 토큰 삭제, prop 제거, 기본 스타일이나 DOM 구조의 비호환 변경

## 컴포넌트 체크리스트

- `co-` 클래스 접두사 사용
- 제품별 문구·URL·분석 로직을 포함하지 않음
- 스타일 덮어쓰기를 위한 `className`과 표준 HTML 속성 전달
- `:focus-visible`, disabled, hover, active 상태 제공
- 390px 모바일과 긴 한국어 문구 확인
- `prefers-reduced-motion` 준수

브랜드가 포함된 헤더와 푸터는 제품 조합 컴포넌트에 남기고, 이 패키지에서는 Button·Container 같은 기반 요소만 제공합니다.
